# 项目协作规则

## 需求确认优先

- 用户提出产品、功能、界面或技术想法时，默认先进入需求讨论，不立即修改代码。
- 在目标用户、使用场景、输入来源、核心流程、范围边界和成功标准得到确认后，必须再次获得用户明确的开发授权，才能开始实现。
- 探索性讨论、需求补充和 PRD 修订不自动构成开发授权。

## 修改前回顾相关历史

- 修改任何已有模块、文件或功能前，先查看其相关 Git 历史，例如 `git log --follow -- <path>`、相关提交详情，以及必要时的 `git blame`。
- 开始修改前应理解该模块此前“改了什么、为什么改”，避免重复、反转或破坏已有设计决策。
- 如果仓库中没有相关历史，应明确记录这是首次实现或首次修改。

## 每次改动立即提交并推送

- 每完成一个完整、可验证且通过隐私审查的原子改动，立即创建 Git commit，并立即 push 到 `origin` 的当前分支；不得把已经完成的改动长期留在本地未提交状态。
- 提交前执行与风险相称的检查，避免推送明显无法运行或不完整的中间状态。
- 每个 commit 只包含同一目的下的相关改动，不把无关修改混入其中。
- commit 标题清楚说明改动内容；commit 正文必须说明改动原因、关键决策或需求依据。
- 如果 push 因网络、权限、认证或远程冲突失败，保留本地 commit，立即说明失败原因，并在阻碍解除后优先完成 push。
- 未经用户明确要求，不重写已推送历史，不强制推送，不删除远程分支。

## 把每次提交都视为未来公开内容

- 当前仓库即使处于私密状态，也必须假设全部 Git 历史最终会公开；私密仓库不能替代隐私与机密审查。
- 真实简历、JD、录音、转写、个人画像、客户资料、商业谈判资料、真实训练数据、凭据和其他敏感输入不得进入 Git 历史，应仅保存在被 `.gitignore` 排除的本地私密目录中。
- 上传前逐个指定需要暂存的文件，不使用 `git add -A`、`git add .` 或其他全量暂存方式。
- commit 前必须检查完整 staged diff、提交作者信息、提交消息、远程地址，并扫描密钥、个人身份信息、本地绝对路径和未公开知识资产。
- 如果文件是否适合未来公开存在任何疑问，停止提交并向用户列出文件与风险，获得明确确认后再继续。
